[package]
name = "nexo-core"
description = "Pure domain types and invariants for NEXO."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[dev-dependencies]
proptest = "1.11"
