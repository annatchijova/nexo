# Test-only image used by nexo-sandbox's own test suite to prove the
# orchestrator's wall-clock timeout actually kills a runaway container.
# Not an extractor; never referenced by application code.
FROM alpine:3
CMD ["sleep", "infinity"]
